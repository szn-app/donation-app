apiVersion: skaffold/v4beta12
kind: Config
metadata:
  name: auth-ory-admin-ui

deploy:
  kubectl: {}

profiles:

- name: development
  manifests:
    kustomize:
      paths:
      - k8s/overlays/dev

- name: production
  manifests:
    kustomize:
      paths:
      - k8s/overlays/prod

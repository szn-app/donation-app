name: test--auth-token-exchange

on:
  pull_request:
    branches: [main]
    paths: ['service/auth-token-exchange/**']
  workflow_dispatch: 

jobs:
    auth-token-exchange:
      needs: []
      # release-please PR originate from branches with `release-please--branches--*`
      if: ${{ !contains(github.event.pull_request.labels.*.name, 'automerge') || github.event_name == 'workflow_dispatch' }}
      
      uses: ./.github/workflows/autogenerated-auth-token-exchange.test.yaml
      secrets: inherit
      # with: 
    
    